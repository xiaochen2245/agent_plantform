# ── 前端环境变量说明（复制为 .env.development / .env.production 按需修改）──
#
# VITE_USE_MOCKS：
#   true  = 开发模式启用 MSW mock（默认，无需后端即可跑通全部页面）
#   false = 走真实后端（vite proxy /api → http://localhost:8000，见 vite.config.ts）
# 快捷方式：npm run dev:real 等价于 VITE_USE_MOCKS=false vite
#
# 本地覆盖请使用 .env.development.local / .env.production.local（勿提交，已被 .gitignore 忽略）
VITE_USE_MOCKS=true
